# T0002

Title: Lock performance baseline to verified facts only
Domain: Platform
Epic: EPIC 00: Platform Governance and Baseline
Type: docs
Priority: P0
Estimated size: S
Owner type: platform
Problem: The current baseline docs mix verified observations with still-unmeasured assumptions, and some repo counts already drifted.
Goal: Finalize the baseline so it contains only reproducible facts, explicit gaps, and clear measurement follow-ups.
Scope: Documentation only. Do not add runtime instrumentation in this ticket.
Allowed files:
- `docs/performance/baseline.md`
- `docs/Repo_Current_State.md`
- `docs/Known_Issues_And_Followups.md`
- `docs/roadmap/sprint-1-execution-plan.md`
Do not touch:
- `src/`
- `prisma/`
- `.github/`
- dependencies
Acceptance criteria:
- the baseline doc clearly separates verified facts from `Needs measurement`
- only numbers that can be reproduced from repo search or build output remain unqualified
- measurement gaps point to the next ticket instead of pretending the data already exists
Manual verification:
- rerun the repo-count and build-snapshot commands referenced in the doc
- confirm any unverified statement is explicitly marked `Needs verification` or `Needs measurement`
Commands to run:
```powershell
npm run build
(Get-ChildItem src\\app\\api -Recurse -File | Measure-Object).Count
(Get-ChildItem src\\app -Recurse -Include page.tsx,page.ts,page.jsx,page.js -File | Measure-Object).Count
rg -l "use client" src | Measure-Object | Select-Object -ExpandProperty Count
```
Risks:
- stale numbers if the baseline is not re-checked before merge
Dependencies:
- none
Rollback plan:
- revert the doc updates if any claim cannot be reproduced

## Architecture scorecard

| Metric | Score | Note |
| --- | --- | --- |
| Performance | +1 | reduces measurement ambiguity |
| Complexity | 0 | docs only |
| Tech Debt | +1 | fixes stale assumptions before coding |
| Bundle Size | 0 | no client impact |
| Maintainability | +1 | improves planning accuracy |
